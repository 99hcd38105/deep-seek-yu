# deep seek yu 客户端

## Windows

双击 Windows 安装包后会打开标准安装向导。安装时可以选择安装路径，并决定是否创建桌面快捷方式、是否随 Windows 开机自动启动。开始菜单快捷方式始终创建；安装完成页还可以选择是否立即运行客户端。

桌面快捷方式指向真正的 `deep seek yu.exe`，不是 Edge/Chrome 快捷方式；客户端会在需要时自动启动本机 Harness 与手机网关。

主程序：`windows/dist/deep seek yu-win32-x64/deep seek yu.exe`

### v1.1.0 测试3版：deep seek yu 品牌版

Windows 客户端、Android 应用、安装包、快捷方式、托盘、桌宠和 Harness 页面统一显示为 `deep seek yu`。GitHub 仓库和发布文件使用不带空格的 `deep-seek-yu`；内部应用标识保持兼容，因此从旧版升级不会清空本机密钥、模型缓存和会话记录。

客户端启动后会显示第一个内置桌宠。桌宠下方会同步显示当前会话、工具执行进度和最终回复，也可直接输入消息；桌宠会按“待机、思考、执行命令、完成、错误”切换动作。可从顶部“桌宠”菜单打开设置，调整显示、聊天面板、置顶、大小、透明度和角色。

在主聊天框点击 Harness 原生的“＋”并选择图片即可自动识图，不显示单独的识图按钮。图片缩略图、附件存储和会话记录全部由 Harness 自己管理；当 DeepSeek 不支持图片时，Host 内部的本地免费视觉服务生成描述，DeepSeek 适配器只发送描述文本。原图不会发送给 DeepSeek 或第三方，普通文件仍走 Harness 原流程。

识图不需要额外 API Key。第一次使用会下载约 270 MB 的 SmolVLM 轻量模型，之后可以离线识图；桌宠设置会显示下载、加载和识别状态。手机端选择的图片通过受保护的局域网连接进入同一个 Harness 内部流程，不再使用网页注入脚本或手机专用识图接口。

默认点击主窗口右上角“×”只会将客户端隐藏到 Windows 托盘，桌宠和当前 Harness 会话继续运行。需要彻底关闭时，从托盘图标菜单选择“退出”；也可以在桌宠设置中关闭后台运行。

后续可以添加自定义桌宠：在桌宠设置中打开自定义目录，为每个角色建立独立文件夹并放入 `pet.json` 与动作图片。缺少某个动作时会自动使用待机图片。

## Android

发布版 APK 文件名为 `deep-seek-yu-Android-1.1.0-beta.3.apk`。

手机与电脑在同一 Wi-Fi 时，也可以直接用手机浏览器打开 Windows 客户端提供的 Android 安装链接下载，无需 USB 数据线。

1. 把 APK 发送到 Android 手机并安装；如果系统询问，只对本次使用的文件管理器允许“安装未知应用”。
2. 电脑先打开 `deep seek yu`，手机和电脑连接同一个可信 Wi-Fi。
3. Windows 客户端首次启动时先填写自己的 DeepSeek API Key；客户端随后会随机生成本机端口和独立的手机连接保护密钥。
4. 点击“手机连接 → 开启手机连接”，复制它生成的完整地址；在 Android 应用右上角点“服务器”并粘贴。

Windows 与 Android 都连接电脑上的同一个 Harness 服务，因此工作区、会话列表和聊天记录保持一致。

选择工作区文件夹时会显示应用内目录浏览器；它在 Windows 客户端和 Android 手机端使用相同的安全目录接口，不依赖容易在打包环境中退出的 Win32 原生文件夹对话框。

首次升级到 Windows 1.0.7 时，客户端会重新生成一次随机端口和手机连接地址；请在手机端重新粘贴新地址。使用托盘菜单完全退出 Windows 客户端后，由它启动的 Harness 和手机入口都会停止。

## 安全范围

- Harness 本体只监听 `127.0.0.1` 上的随机端口，不会直接暴露到局域网。
- 每台电脑首次运行时由使用者填写自己的 DeepSeek API Key；API Key 只写入该电脑专用的 Harness 凭据目录，不会写入源码、Android APK 或安装包。
- 客户端会自动生成随机手机端口和独立的手机连接保护密钥，两者也只保存在本机应用数据目录。
- 手机入口默认关闭；只有点击“开启手机连接”后才会监听随机端口，并且只接受当前局域网网段和正确密钥。
- 只在可信家庭/个人 Wi-Fi 使用；不要做公网端口映射，也不要在公共 Wi-Fi 开启手机入口。
- 电脑关闭或客户端/Harness 未运行时，Android 客户端无法连接，这是正常现象。
- 本地视觉模型从 Hugging Face 官方仓库下载；图片数据不随模型下载请求上传。旧版保存的豆包/火山方舟视觉密钥会在升级后移除。

## GitHub 版本发布

仓库使用 GitHub Releases 保存可下载的安装包，不把大型二进制文件写进 Git 历史。发布新版本时，先把 Windows 与 Android 的版本号改成同一个值并推送源码，再创建并推送对应的 `v版本号` 标签。GitHub Actions 会自动构建并发布 Windows 安装包、Android APK 和 SHA-256 校验文件。

公开仓库中的 Releases 可供任何人下载，但只有仓库所有者和被授权协作者能够直接推送修改。
